# Antigravity 主题改造工程总览与开发指南

欢迎来到 **Antigravity 主题与体验定制工程**（工作目录：`d:\work\antigravity-background`）。

本项目整合了历史对话 **「背景替换」** 与 **「左侧任务栏样式调整」** 的全部研究成果、核心代码、自动化运维脚本、逆向分析工具以及主题资源。本工程作为后续 Antigravity 桌面端主题改造、汉化更新与功能扩展的统一工程底座。

---

## 一、工程目录架构全景

```text
d:\work\antigravity-background\
├── 工程总览与使用指南.md                     # [本文件] 项目主入口与开发使用手册
│
├── 主题样式\                                 # 主题 CSS 样式与壁纸核心资产
│   ├── 晨雾森林毛玻璃主题.css                # 深度优化的毛玻璃主题样式文件（含任务栏与对话框透明规则）
│   ├── 壁纸原图.jpg                          # 2560x1440 晨雾森林标准壁纸
│   ├── 壁纸原图_高保真.png                   # 原始 2.34MB 无损高保真壁纸原图
│   └── 主题配置项.json                       # BetterGravity 对应的主题与插件启用配置参考
│
├── 汉化插件\                                 # Antigravity 2.0 深度汉化插件源码
│   ├── plugin.json                           # 插件元数据配置
│   └── index.js                              # 动态 DOM 监听 (MutationObserver) 汉化核心逻辑
│
├── 核心脚本\                                 # 部署、同步与版本恢复自动化脚本
│   ├── 一键同步到系统.ps1                    # 将本工程最新 CSS/插件一键部署到 %APPDATA%\BetterGravity
│   ├── 从系统同步到工程.ps1                  # 将系统当前的最新状态拉取备份回本工程
│   ├── 准备恢复补丁.js                       # 针对新版本 Antigravity 自动化构建引导代理包
│   ├── 自动恢复.ps1                          # 安全关停、等待文件锁解除、覆盖 asar 并拉起客户端
│   ├── restore.ps1                           # 自动恢复脚本的纯 ASCII 命名副本
│   └── 一键完成恢复并启动.bat                # 兼容 Windows GBK 终端的双击运行恢复入口
│
├── 逆向与调试工具\                           # 客户端结构逆向与选择器提取工具
│   ├── 解析客户端界面.js                     # 分析 app.asar 前端入口与资源加载逻辑
│   ├── 提取界面选择器.js                     # 从 language_server.exe 二进制提取 testid 与 CSS 结构
│   └── 优化晨雾主题样式.js                   # 自动化样式调整与注入工具
│
├── 文档与方案\                               # 详尽的专项方案与技术白皮书
│   ├── 晨雾森林毛玻璃主题使用说明.md         # 晨雾森林主题各区域透明度与参数微调手册
│   ├── Antigravity更新2.17.0后BetterGravity恢复指南.md # 官方版本更新后的恢复机制与避坑指导
│   ├── BetterGravity汉化插件使用与配置说明.md # 汉化词典结构、动态变量匹配与配置指南
│   └── 界面样式逆向与任务栏透明化方案.md     # 侧边栏灰色遮罩及主输入框透明化的逆向与穿透方案
│
└── 历史素材与截图\                           # 演进过程中的排查证据与效果对比图
    ├── 壁纸源站详情截图.png
    ├── 初始纯黑全屏遮挡排查图.png
    ├── 跨协议沙箱安全拦截排查图.png
    ├── 更新恢复CMD编码排查图.png
    ├── 界面样式调整需求对比图.jpg
    └── 左侧任务栏灰色矩形遮罩特写.png
```

---

## 二、日常主题开发与调试工作流

在此工程中进行样式调整或插件开发非常高效，遵循以下标准工作流：

### 1. 修改主题样式
直接编辑 [`主题样式/晨雾森林毛玻璃主题.css`](file:///d:/work/antigravity-background/%E4%B8%BB%E9%A2%98%E6%A0%B7%E5%BC%8F/%E6%99%A8%E9%9B%BE%E6%A3%AE%E6%9E%97%E6%AF%9B%E7%8E%BB%E7%92%83%E4%B8%BB%E9%A2%98.css)：
* **调节透明度**：修改对应的 `rgba(r, g, b, 0.xx)` 不透明度数值。
* **调节模糊程度**：修改 `backdrop-filter: blur(16px)` 中的像素值。
* **更换壁纸**：将新壁纸转换为 Base64 编码，替换 CSS 顶部 `background-image: url("data:image/jpeg;base64,...")` 中的数据。

### 2. 一键同步部署到系统
在当前工程根目录执行一键同步脚本：
```powershell
powershell -ExecutionPolicy Bypass -File .\核心脚本\一键同步到系统.ps1
```
脚本将全自动把修改后的 CSS、壁纸配置和汉化插件推送至 `%APPDATA%\BetterGravity\`。

### 3. 即时热重载生效
无需重启客户端，在 **Antigravity 2.0 桌面端窗口** 中直接按下：
<kbd>Ctrl</kbd> + <kbd>R</kbd>
客户端将瞬间重新加载最新 CSS 样式与插件逻辑！

---

## 三、版本更新（如 2.17.0+）后的全自动恢复机制

当 Antigravity 官方发布版本更新时，其安装包会覆盖 `resources/app.asar`，导致 BetterGravity 的引导入口被还原为官方纯净版。恢复流程如下：

1. **构建新版适配补丁**：
   在终端执行构建脚本：
   ```powershell
   node .\核心脚本\准备恢复补丁.js
   ```
   脚本将基于官方更新后的 `_app.asar` 提取最新版本号与哈希，重新打包生成轻量级引导启动包 `app.asar.bettergravity-staged`。

2. **一键替换与重启**：
   直接双击运行：
   👉 [`核心脚本/一键完成恢复并启动.bat`](file:///d:/work/antigravity-background/%E6%A0%B8%E5%BF%83%E8%84%9A%E6%9C%AC/%E4%B8%80%E9%94%AE%E5%AE%8C%E6%88%90%E6%81%A2%E5%A4%8D%E5%B9%B6%E5%90%AF%E5%8A%A8.bat)
   * 自动优雅退出 6 个并发客户端进程；
   * 循环检测系统文件句柄，确保内存释放；
   * 瞬间将补丁覆盖为 `app.asar`；
   * 自动重新拉起 Antigravity，全面恢复壁纸主题与汉化。

---

## 四、历史攻坚与底层技术沉淀

在历次演进中，我们总结并沉淀了以下关键技术机制与避坑经验：

| 问题现象 | 根本技术成因 | 最终工程化解决方案 |
| :--- | :--- | :--- |
| **仅侧边栏有变化，主窗口纯黑** | Antigravity 2.0 顶层存在 `<div class="h-screen w-screen bg-background">` 纯黑全屏遮罩 | 通过 CSS 穿透强制将顶层外壳与容器设置为 `background: transparent !important` |
| **主题变深绿但未显示壁纸原图** | Chromium 本地协议安全沙箱限制（`https://127.0.0.1` 严禁读取 `file:///` 本地图片） | 将高清壁纸转为 Base64 内嵌至 CSS，彻底规避跨域沙箱拦截，实现 0 延迟渲染 |
| **Windows 批处理执行卡死黑屏** | CMD.exe 默认代码页为 GBK（CP936），若 `.bat` 包含 UTF-8 中文字符会导致命令被截断损坏 | 编写纯 ASCII 批处理作为轻量入口，将复杂逻辑委托给 UTF-8 编码的 PowerShell 执行 |
| **替换补丁提示文件被占用 (EBUSY)** | Windows NT 内核对运行中 Electron 进程持有独占只读句柄，进程退出后内核释放有 1.5~2 秒延迟 | 在 PowerShell 脚本中加入 `Stop-Process` 退出检测与 15 次重试循环等待机制 |
| **任务栏列表项有浅灰色矩形遮罩** | 侧边栏列表项目自带 Tailwind 混淆背景类（如 `bg-accent/40`, `bg-muted`） | 在主题 CSS 中针对 `aside button`、`[data-testid="conversation-list-sidebar"]` 等设置背景透明，仅保留 hover 微光 |
| **新建对话输入框发灰不透明** | `[data-testid="agent-input-box"]` 内部绑定了 `--card` 等局部 CSS 自定义属性 | 将输入框底色设为 `rgba(20, 24, 28, 0.45)`，并强制覆盖 `--card: transparent !important` |

---

## 五、各模块详细文档索引

* 📖 **主题设计与参数说明**：[`文档与方案/晨雾森林毛玻璃主题使用说明.md`](file:///d:/work/antigravity-background/%E6%96%87%E6%A1%A3%E4%B8%8E%E6%96%B9%E6%A1%88/%E6%99%A8%E9%9B%BE%E6%A3%AE%E6%9E%97%E6%AF%9B%E7%8E%BB%E7%92%83%E4%B8%BB%E9%A2%98%E4%BD%BF%E7%94%A8%E8%AF%B4%E6%98%8E.md)
* 📖 **版本更新与补丁恢复**：[`文档与方案/Antigravity更新2.17.0后BetterGravity恢复指南.md`](file:///d:/work/antigravity-background/%E6%96%87%E6%A1%A3%E4%B8%8E%E6%96%B9%E6%A1%88/Antigravity%E6%9B%B4%E6%96%B02.17.0%E5%90%8EBetterGravity%E6%81%A2%E5%A4%8D%E6%8C%87%E5%8D%97.md)
* 📖 **汉化插件与字典扩展**：[`文档与方案/BetterGravity汉化插件使用与配置说明.md`](file:///d:/work/antigravity-background/%E6%96%87%E6%A1%A3%E4%B8%8E%E6%96%B9%E6%A1%88/BetterGravity%E6%B1%89%E5%8C%96%E6%8F%92%E4%BB%B6%E4%BD%BF%E7%94%A8%E4%B8%8E%E9%85%8D%E7%BD%AE%E8%AF%B4%E6%98%8E.md)
* 📖 **任务栏透明化与逆向方案**：[`文档与方案/界面样式逆向与任务栏透明化方案.md`](file:///d:/work/antigravity-background/%E6%96%87%E6%A1%A3%E4%B8%8E%E6%96%B9%E6%A1%88/%E7%95%8C%E9%9D%A2%E6%A0%B7%E5%BC%8F%E9%80%86%E5%90%91%E4%B8%8E%E4%BB%BB%E5%8A%A1%E6%A0%8F%E9%80%8F%E6%98%8E%E5%8C%96%E6%96%B9%E6%A1%88.md)
